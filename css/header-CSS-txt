    body, html {
   padding: 0;
   margin: 0;
   height: 100vh;
   min-height: 100vh;  // for extra vigilance because some browsers are finicky
    }

    .svg-container {
      width: 100%;
      height: 100vh; /* Imposta l'altezza per riempire la viewport */
      display: flex;         /* Utilizza flexbox per il centering */
      justify-content: center; /* Centra orizzontalmente */
      align-items: center;    /* Centra verticalmente */
    }

    .svg-container svg {
      width: auto;          /* Mantiene le proporzioni originali */
      max-width: 80%;        /* Imposta una larghezza massima per evitare che sia troppo grande */
      max-height: 80%;       /* Imposta un'altezza massima */
    }
